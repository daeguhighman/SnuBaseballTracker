// // CustomInput.jsx
// import React from "react";

// const CustomInput = React.forwardRef(({ value, onClick }, ref) => (
//   <button className="my-custom-input" onClick={onClick} ref={ref}>
//     {value || "날짜 선택"}
//   </button>
// ));

// export default CustomInput;
